

mixin slider(name, min, max, val1, val2, sliderAttrs)
  .slider(
  data-min=min 
  data-max=max 
  data-val1=val1 
  data-val2=val2)&attributes(attributes)
    .slider__label text
    input(type="hidden" name=name + "[0]")
    input(type="hidden" name=name + "[1]")
    .ui-slider