include ../checkbox/checkbox.pug

mixin rich-checkbox(name, title, checked, rcbAttrs, cbAttrs)
  +checkbox(name, checked, join({class: "rich-checkbox"}, rcbAttrs), cbattrs)
    .rich-checkbox__title= title
    block
